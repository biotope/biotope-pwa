{
  "name": "@biotope/pwa",
  "version": "0.1.2",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "start": "http-server ./demo -S -C cert.pem -o ",
    "build": "echo \"build\"",
    "release": "np",
    "prepublishOnly": "npm run clean && npm install",
    "clean": "node_modules/.bin/rimraf node_modules",
    "test": "echo \"No tests found\"",
    "cert": "openssl req -newkey rsa:2048 -new -nodes -x509 -days 3650 -keyout key.pem -out cert.pem",
    "docs": "docsify serve docs"
  },
  "dependencies": {
    "hygen": "^1.6.2",
    "inflection": "^1.12.0",
    "inquirer": "^6.5.0",
    "web-push": "^3.3.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/biotope/biotope-pwa.git"
  },
  "engines": {
    "node": ">=8.9.0"
  },
  "devDependencies": {
    "chalk": "^2.4.2",
    "docsify-cli": "^4.3.0",
    "http-server": "^0.11.1",
    "np": "^5.0.3",
    "rimraf": "^2.6.2"
  }
}
